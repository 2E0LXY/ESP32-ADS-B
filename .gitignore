.pio/
credentials.json
*.key
*.pem
release/*.bin
docs/firmware/

# Generated by the pioarduino IDF-libs build that custom_sdkconfig turns on.
# These appear in the project root the first time an environment rebuilds
# ESP-IDF from source; none of them are inputs, all are reproducible.
.dummy/
managed_components/
dependencies.lock
sdkconfig
sdkconfig.*
